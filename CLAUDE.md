# CLAUDE.md

このリポジトリで作業するときのルールです。Claude Codeはこのルールに従ってください。

## このプロジェクトについて

git・Docker・Java(Spring Boot)・Linux・AWS をクイズ形式で学べる学習用アプリです。
ユーザー自身の学習を兼ねているため、通常のプロダクト開発以上に「わかりやすさ」を重視します。

## ルール

1. **説明はすべて日本語で行う**
   会話での説明、コミットメッセージ、コード内コメントはすべて日本語で書く。

2. **一度に大きく作らず、小さい単位で進める**
   1つの変更(1コミット/1ステップ)でやることを絞る。
   複数の機能をまとめて一気に実装しない。次に何をするか都度説明してから進める。

3. **各ファイルの役割と「なぜそう書くか」を初心者にもわかるように説明する**
   新しいファイルを作るときは、そのファイルが何をするためのものか、
   なぜそのような設計・書き方をしたのかをコメントやREADMEで説明する。
   「何をしているか」だけでなく「なぜそう書くのか」を意識する。

4. **秘密情報(APIキーなど)は絶対にコミットしない**
   `.env` やAWSの認証情報などは `.gitignore` で除外し、
   コード内にも直接書き込まない。コミット前に差分を確認する。

## 現在の構成

- Spring Boot 3 / Java 21 / Maven
- `GET /api/quizzes?category=git` で問題一覧を取得するAPIを実装済み
- 問題データはPostgreSQL(Docker Composeで起動)にJPA経由で保存
  (起動時に `data.sql` で初期データを投入。再起動しても重複しない)
- 問題は暗記系のトリビアではなく、実務のトラブル対応・設計判断を問う内容にしている
  (各カテゴリ5問、計25問)。回答後には「なぜその答えなのか」の解説(explanation)を表示する
- テスト実行時はPostgreSQLの代わりにインメモリDB(H2)へ自動で差し替わる設定済み
- `src/main/resources/static/` 配下に簡単なフロントエンド(HTML/CSS/JS)を実装済み
  (カテゴリ選択→1問ずつ出題→正誤判定→結果表示)

## 長期的な方向性

このアプリは最終的に、バックエンド領域(git・Docker・Java・Linux・AWSに限らず、
DB・ネットワーク・セキュリティなど)を幅広く学べる、規模の大きい学習アプリに
育てていきたいという想定があります。
ただしルール2の通り、今すぐそれらを作り込むことはせず、
必要になったタイミングで1つずつ小さく追加していきます。
カテゴリをenumで管理しているのは、この「後から科目を増やしやすくする」ための設計です。

## 今後の進め方(想定)

小さいステップで少しずつ機能を追加していく想定です。例:
- カテゴリ一覧を返すAPIを追加する
- 正解判定APIを追加する
- data.sqlをFlywayなどのマイグレーションツールに置き換える
- アプリ本体もDocker化する(現状はDBのみDocker化済み)
- 新しい学習カテゴリ(DB・ネットワークなど)を追加する

これらは今回のタスクの範囲外なので、指示があるまで実装しません。
